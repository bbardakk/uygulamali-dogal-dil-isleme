# Quarto build outputs (rendered site is produced by CI)
/.quarto/
/en/.quarto/
/tr/.quarto/
/en/_book/
/tr/_book/
/public/
/site/

# NOTE: _freeze/ is intentionally NOT ignored.
# Committed freeze results let CI publish without re-executing notebooks.

# Python
__pycache__/
.ipynb_checkpoints/
.venv/

# OS / editor
.DS_Store

# Working files from Claude Code sessions: review reports and drafts that
# are read once and acted on, not part of the book.
/Claude outputs/
